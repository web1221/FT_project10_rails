<h2>Welcome!</h2>
<h3>Mario's Super Store is a one stop shop til you drop. You can find all your needs here! From Organic Candles to Jazz posters. Never shop anywhere else again!</h3>

<h4>Here are the three most recently added products:</h4>

  <% @products.three_most_recent.each do |p| %>
    <ul>
      <li>
        <%= link_to p.name, product_path(p) %>
      </li>
    </ul>
  <% end %>
  <h4>Here are products made in the United States:</h4>
  <% if @products.any? %>
    <ul>
      <% @products.usa_product.each do |p| %>
        <li><%= link_to p.name, product_path(p) %></li>
      <% end %>
    </ul>
  <% else %>
    <p>There are no products made in the United States yet.</p>
  <% end %>

  <p><%= link_to "Products", products_path %></p>
